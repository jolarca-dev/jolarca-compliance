"""Retention engine — retention policy as executable code (STEP 30).

Creed: retention policy is only real if a job runs it and a test proves it.
Single source of truth: retention/matrix.yaml. Stdlib + PyYAML only.
"""
