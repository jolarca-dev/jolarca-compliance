# SOC 2 — Evidence by Trust Services Criterion

One subfolder or index file per criterion family (CC1–CC9, plus A/C/P/PI as
applicable). Each evidence item links: criterion → policy requirement →
artifact path → registry hash.

Convention: `CC<n>-<slug>.md` index entries; binaries via LFS pointer + hash.
Mapping source: `docs/compliance-matrix.md`.
